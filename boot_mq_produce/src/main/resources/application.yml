server:
  port: 7777

spring:
  activemq:
    broker-url: tcp://127.0.0.1:61616
    user: admin
    password: admin
  jms:
    pub-sub-domain: false     #false = Quene   true = Topic

#自己定义队列名称
myqueue: boot-activemq-quene